# Weijia (Charlie) Zhang

Personal website for Weijia (Charlie) Zhang, built with Jekyll and the AcademicPages template.

Local development:

```bash
bundle exec jekyll serve
```

The old al-folio source lives in `old_personal_website/` as migration input and is excluded from the generated site.
